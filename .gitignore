# 仓库根级忽略（前端应用自身的忽略规则见 web/.gitignore）

# 系统 / 编辑器
.DS_Store
*.log

# 本地工具与部署产物（含本地路径/服务器信息，不入库）
.claude/
deploy/

# 二次开发的临时工具与素材（模型压缩工具、原始大模型等，不入库）
交付物/
# 原作者模型备份（大文件，本地留存即可）
web/public/models/*.original-sen-backup
# 本地开发笔记（个人留存，不入库；版本历史见 CHANGELOG.md）
改造记录.md

# Blender 自动备份
*.blend1
*.blend2
